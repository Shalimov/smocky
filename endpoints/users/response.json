{
  "GET": {
    "status": 200,
    "body": {
      "users": "{{ db.users.all }}",
      "activeUsers": "{{ db.users.where active=true }}",
      "filter": "{{ req.query.filter }}"
    }
  },
  "POST": {
    "status": 201,
    "body": "{{ req.body }}"
  }
}
